# Claude Code 配置指南 — 项目规范

本项目是 Claude Code 通用配置指南文档库，不是代码项目。

## 文档结构

- `00-04`: 核心指南文档（日常使用/CLAUDE配置/Hooks/Skills/工作流）
- `prompt-*.md`: 执行用 prompt（初始化/迁移/升级/更新）
- `README.md`: 目录总览 + 版本记录
- `research/`: 调研资料（不随版本更新）

## 版本规范

- 当前版本写在每个文档头部和尾部的 `**版本**: vX.Y`
- 版本号全局统一：所有 00-04 文档 + README 必须同版本
- 版本递增规则：功能新增/结构调整 = 小版本 +1（如 v3.12 → v3.13）
- 版本记录写在 `README.md` 的版本记录表中，一行概括变更内容

## 修改时必须同步的内容

以下信息在多个文件中出现，修改时 MUST 全局同步：

| 信息 | 涉及文件 |
|------|---------|
| 版本号 | 00-04 全部（头+尾）、README |
| Hook 事件总数 | 02（正文）、README（文档列表+新增表）、prompt-更新指南规范 |
| Skills 总数 | README（版本记录） |
| 章节编号 | 修改所在文档的目录 + 正文标题编号 |
| 新版本功能 | README（版本记录）、prompt-guide版本升级（新功能知识+检查项） |

## 编写规范

- 表格列之间 MUST 对齐（中文内容用空格补齐）
- 代码块 MUST 标注语言（```bash / ```json / ```markdown / ```yaml）
- 用 MUST / MUST NOT / SHOULD 表达约束强度（RFC 2119）
- 跨文档引用格式："详见文档 0X Section Y" 或 "详见文档 0X 的 [章节名]"
- Skill 模板用四重反引号 `````markdown` 包裹（防止嵌套冲突）

## 修改流程

1. 先讨论确认方向，再动手改
2. 改完用 Grep 验证全局一致性（版本号、数量引用等）
3. 提交信息格式：`feat: vX.Y 一句话概括` 或 `fix: vX.Y 修复内容`
