# 工作流最佳实践

> 2026 年 Claude Code 高效协作工作流 — 基于社区实践总结

**版本**: v3.41
**适用**: Claude Code 2.x（2026 年）

---

## 目录

1. [核心开发循环](#1-核心开发循环)
2. [Plan Mode 使用规范](#2-plan-mode-使用规范)
3. [并行开发：Git Worktrees](#3-并行开发git-worktrees)
4. [Agent Teams：多 Claude 协作](#4-agent-teams多-claude-协作)
5. [MCP 服务器选型](#5-mcp-服务器选型)
6. [GitHub Actions 集成](#6-github-actions-集成)
7. [开发文档体系](#7-开发文档体系)
8. [上下文管理](#8-上下文管理)
9. [子代理与并发任务](#9-子代理与并发任务)
10. [代码质量防御](#10-代码质量防御v313)
11. [代码健康诊断](#11-代码健康诊断v315)
12. [反模式：避免这些坑](#12-反模式避免这些坑)
13. [快速参考卡](#13-快速参考卡)

---

## 1. 核心开发循环

### 前置阶段：需求讨论（按需）

并非每个功能都需要长时间讨论。简单功能直接进入开发循环即可。但当功能复杂时——需要多轮讨论需求、联网调研技术方案、敲定 UI 交互细节、理清业务逻辑——应该先把讨论成果落盘，再开始编码。

**何时需要这个阶段**：
- 需要多轮讨论才能明确"到底要做什么、怎么做"
- 涉及联网搜索、技术选型对比
- UI/交互设计需要逐页面、逐按钮确认
- 业务逻辑复杂，边界情况多

**工作流**：

```
讨论需求/设计（多轮，方向逐渐明确）
   ↓
方向已定（细节可留待后续）→ 【初稿时机】
   ↓
/spec [功能名]        ← 整理为 docs/specs/xxx.md（status: draft）
                     ← Gate 带类型标注（auto/command/manual）
                     ← 分歧项用 AskUserQuestion 确认
   ↓
/clear                ← 清空上下文
   ↓
┌──────────────────────────────────┐
│ 两种走向                           │
│  ├── 还要继续讨论                  │
│  │     "读取 docs/specs/xxx.md，   │
│  │      继续讨论 [未确定部分]"      │
│  │     → 讨论后再 /spec 增量更新   │
│  └── 方案敲定 → 【定稿时机】        │
│        /spec xxx 确认              │
│         → AskUserQuestion 弹窗     │
│         → status: draft → approved │
│         → 进入实施                 │
└──────────────────────────────────┘
```

**关键原则**：
- Spec 支持增量更新，讨论可跨多个上下文窗口（Brooker 2026："spec 是被迭代的对象"）
- `/spec` 不要求讨论完整——draft 状态先保存，下次继续完善
- **初稿时机**：方向已定即可写，不必全部细节都清晰
- **定稿时机**：实施前明确"确认"，切换 approved
- 简单功能不需要 spec，用 `/implement` 直接执行（详见下方）
- 大型项目可按模块建子目录（如 `docs/specs/auth/`）

**文档边界**（Spec vs 其他）：
- **Spec**（/spec 产出）= 执行契约（做什么 + 怎么验证完成）
- **PRD** = 问题是什么
- **RFC** = 提议方案征求评审
- **ADR** = 架构决策不可变记录（**不合并进 spec**，spec 里引用链接即可）

---

### 单改动实施：`/implement`（v3.19，前身 `/task`）

并非所有改动都需要 Spec。实际开发中大量工作是**方向明确、可直接实施**的单个改动：

- 业务方需求："列表加个排序"、"把默认值改成 20"
- 小 Bug："日期格式显示不对"、"某按钮点击没反应"
- 技术改进："加个索引优化查询"、"升级某个依赖版本"

但**小改动不等于小心思**——每个"小改动"都藏着 3-5 个架构决策点（要不要复用现有实现？放哪一层？和项目命名是否一致？）。如果每次都走最短路径，几个月后就会累积出 5 个 sort 实现、3 种命名风格的面条代码。

`/implement` 的定位是"**有纪律的快速执行**"：压缩流程但**不丢关键检查点**（模式扫描、架构边界、Tidy First）。

```bash
/implement 列表页加个按名称排序     # 单任务
/implement                          # 批量模式，列出多个独立走流程
```

**`/implement` 内部流程**：

```text
Step 1: 复杂度评估（硬阈值）
   任一触发 → 建议 /spec 或 Plan Mode
   - ≥3 文件（无法一句话描述 diff）
   - 跨模块/跨层 import
   - 新增第三方依赖
   - 改变数据流向（API / state / DB schema）
   ↓
Step 2: 模式扫描（MUST）
   - rg 搜索现有实现（sort/filter/format...）
   - 找到相似实现 → MUST 说明"为什么不复用"
   - 发现 3+ 种风格 → 提醒统一
   ↓
Step 3: 按复杂度执行
   - 简单（1-2 文件）→ Code → Verify → Commit
   - 中等（3-5 文件）→ Explore → Code → Verify → Simplify（建议）→ Commit
   - Bug → Explore(复现) → Code(集成测试重现 → 修复) → Verify → Commit
   ↓
Step 4: Verify
   ↓
Step 5: Commit 前自检（MUST，防面条关键）
   Kent Beck 三红灯（任一 → 暂停汇报）:
     - 循环/重试掩盖失败？
     - 加了用户没要求的功能？
     - 禁用/删除了测试？
   Tidy First:
     - 同时有结构变动 + 行为变动？
     - 是 → 拆两个 commit（结构先，行为后）
   ↓
Step 6: Commit（Conventional Commits）
   ↓
Step 7: ADR 触发检查（条件弹窗）
   四类触发（跨模块依赖/替换实现/新依赖/数据流改变）
   → AskUserQuestion 弹窗询问是否生成 ADR 草稿
   ↓
Step 8: /docs 联动提示（如涉及架构敏感区）
```

**何时用 `/implement`，何时用 `/spec`**：

| 信号 | 用 `/implement` | 用 `/spec` |
|------|---------------|-----------|
| 一句话能说清楚要做什么 | ✅ | |
| 改动范围可预估（1-2 文件） | ✅ | |
| 跨 3+ 文件或跨模块 | | ✅ |
| 新增第三方依赖 | | ✅ |
| 改变数据流向（API/schema/state 形状） | | ✅ |
| 需要多轮讨论才能明确需求 | | ✅ |
| 涉及架构设计、新模块 | | ✅ |
| 需要 UI 设计、API 设计讨论 | | ✅ |

**批量模式**：一个会话连续处理多个改动，**每个独立走完整 Step 1-8**（批量 ≠ 简化流程）。单个失败 → 停止批量汇报；上下文 > 60% → 提醒分批。

**跟踪**：单改动不需要 Roadmap 条目和 Spec 文件，commit history + PR 就是追溯记录。如果关联 Roadmap 条目，`/implement` 会在输出末尾提示"如需更新，执行 `/done <描述>`"。

**设计参考**：
- Anthropic 官方 "search before implement" 模式（agent 每任务跑 10-30 次 rg）
- Kent Beck [Augmented Coding](https://tidyfirst.substack.com/p/augmented-coding-beyond-the-vibes)（三红灯 + Tidy First）
- Claude Code 最佳实践的 "≥3 文件 → plan" 阈值

详细的 Skill 模板见文档 03 Section 2.6。

---

### 大 Spec 分阶段实施（v3.9）

当 Spec 内容较多时（多个模块、预估实施超过一个上下文窗口），按 Phase 分阶段实施，避免中途 auto-compact 导致进度丢失。

**核心思路**：Spec 文件本身就是进度追踪器——Claude 边做边勾 `[x]`，compact 后重新读 spec 即可恢复。

```
/spec 生成 spec（含 Implementation Phases）
   ↓
/clear
   ↓
"读取 docs/specs/xxx.md，开始实施 Phase 1"    ← 每次只实施一个 Phase
   ↓
Claude 实施 Phase 1：
  - 逐个完成 Tasks，每完成一个勾 [x]
  - 所有 Tasks 完成 → 检查 Gate 条件
  - Gate 全通过 → 更新 spec 的 active_phase
  - 建议执行 /done
   ↓
Phase 完成后两种走向：
├── 上下文充裕（< 50%）→ 继续 "开始实施 Phase 2"
└── 上下文紧张（> 50%）→ /handoff → /clear → /catchup → "继续实施 Phase 2"
   ↓
所有 Phase 完成 → /done + /release（如 Roadmap Phase 也完成）
```

**为什么有效**：
- Spec 文件中的 `[x]` 状态持久化在磁盘上，不受 compact 影响
- `active_phase` 字段让 compact 后的 Claude 一眼知道做到哪了
- 每个 Phase 独立可交付，即使 compact 丢失细节也能从 Gate 条件恢复认知
- 讨论和实施分开会话，上下文全部用于编码

**讨论与实施分会话**（推荐）：

| 阶段 | 推荐模型 | 原因 |
|------|---------|------|
| 需求讨论 + `/spec` | Opus | 深度推理，方案质量高 |
| Phase 实施 | Sonnet | 速度快，编码质量足够 |
| 复杂 Phase（架构变更） | Opus | 需要深度思考 |

---

### 开发循环

每个功能进入开发后，遵循六步循环：

```
Explore → Plan → Code → Verify → Simplify → Commit
  探索     规划    编码    验证      简化       提交
```

**并非每个功能都需要走完全部六步。** 根据复杂度选择合适的流程：

| 复杂度 | 判断标准 | 推荐流程 |
|--------|---------|---------|
| 简单 | 能一句话描述改动（如"修个 typo"、"改个样式"） | Code → Commit |
| 中等 | 改多个文件，但方向明确 | Explore → Plan → Code → Verify → Simplify → Commit |
| 复杂 | 涉及架构设计、方向不确定、改动影响面大 | Explore → Plan → **Clear** → Code → Verify → Simplify → Commit |
| Bug 修复 | 修复已知或新发现的 Bug | Explore（复现+定位）→ Plan（根因确认）→ Code（回归测试+修复）→ Verify → Simplify → Commit |

> **复杂功能模式**：Explore + Plan 会消耗大量上下文（通常 50-70%）。此时主动 `/clear` 后带 plan 文件开新会话编码，是**主动策略**而非被动应对。官方文档和社区均推荐此模式。

### Bug 修复变体

Bug 修复复用同一套六步循环，但每步**侧重点不同**：

| 步骤 | 新功能 | Bug 修复 |
|------|--------|---------|
| **Explore** | 了解架构，找切入点 | **复现 Bug**，阅读日志/报错，缩小范围定位根因 |
| **Plan** | 选实现方案 | **确认根因**，评估修复方式对其他模块的影响 |
| **Code** | 先写集成测试 → 实现功能 | 先写**复现集成测试**（从用户视角证明 Bug 存在）→ 修复 → 测试变绿 |
| **Verify** | 功能正确 + 边界覆盖 | Bug 消失 + **回归验证**（确认没引入新问题） |
| **Simplify** | 同 | 同（修复代码也应简洁） |
| **Commit** | `feat:` 前缀 | `fix:` 前缀 |

**实际操作**：

```
你：登录页面在手机端点击提交按钮没有反应

Claude：（Explore — 复现+定位）
  读取登录组件、事件处理逻辑、移动端适配代码...
  发现：onClick handler 使用了 onMouseDown，移动端不触发...

你：确认，修一下

Claude：（Code — 回归测试+修复）
  1. 写复现测试（模拟触摸事件，断言提交被触发）
  2. 将 onMouseDown 改为 onPointerDown
  3. 复现测试变绿

  （Verify — 自动执行）
  边界检查 + 全量测试回归验证...
  验证通过。

  （Simplify — 自动执行）
  修复代码简洁性检查...

  git commit -m "fix(auth): 修复移动端登录按钮无响应"
```

**与新功能的关键区别**：
- Bug 修复的 Explore 阶段更像**侦探工作**——从现象出发逆向追踪到根因
- Code 阶段的测试不是"定义完成标准"，而是"证明 Bug 存在"
- 简单 Bug 可以跳过 Plan，直接 Explore → Code → Verify → Commit
- 复杂 Bug（涉及多模块、难复现）建议用 Plan Mode 分析后再修

### Step 1: Explore（探索）

让 Claude 理解相关代码，**不要让它写任何代码**。

```
明确告诉 Claude："先不要写代码，只读取相关文件，告诉我现在的代码结构"

让 Claude 读取：
- 要修改的文件
- 相关依赖文件
- 测试文件
- 类型定义
- `docs/specs/` 中相关的设计文档（如有，读取作为实施参考，并将状态更新为"实施中"）
```

**为什么先探索**：Claude 不读代码直接写，就像程序员不看现有代码直接改——大概率方向错误或与现有代码冲突。

### Step 2: Plan（规划）

探索后，让 Claude 提出 **2-3 个实现方案**（不是直接写代码）。

```
"基于你对代码的理解，提出 2-3 个实现方案，
 包括各方案的优缺点。先不要写代码。"
```

**评估要点**：
- 与现有代码风格是否一致？
- 对其他模块的影响范围？
- 测试难度？

确认方案后，再进入 Code 阶段。

**复杂功能：Plan 后清理上下文**

如果 Explore + Plan 已消耗 50%+ 上下文（用 `/cost` 检查），推荐主动清理：

```bash
# 1. 让 Claude 将方案写入文件
"将实施方案写入 .claude/plan.md，包括：方案决策、影响的文件、实施步骤"

# 2. 清空上下文
/clear

# 3. 带 plan 开始新会话
"读取 .claude/plan.md，按方案实施"
```

新会话拥有干净的 200k 上下文全部用于编码，质量显著优于在拥挤上下文中继续。

### 测试策略：Testing Trophy（v3.16）

> *"Write tests. Not too many. Mostly integration."* — Guillermo Rauch
>
> 单元测试每个函数都通过，但用户点击分页没反应——这类 Bug 的根因是**测试类型错了**。

传统测试金字塔强调大量单元测试，但现代前后端应用的 Bug 大多出在**组件串联处**，不是单个函数内部。Kent C. Dodds 的 **Testing Trophy** 模型将重心移到集成测试：

```
    E2E 测试        ← 少量：关键端到端路径（登录→CRUD→退出）
  ▐ 集成测试 ▐      ← 主力：用户操作链路（点击→请求→数据→UI 更新）
   单元测试         ← 少量：纯计算逻辑（日期格式化、金额计算）
  静态检查          ← 底座：TypeScript + ESLint（自动覆盖）
```

#### 四层测试职责

| 层 | 测什么 | 前端工具 | 后端工具 | 占比 |
|---|---|---|---|---|
| 静态 | 类型错误、语法 | TypeScript + ESLint | mypy/Pylint、编译检查 | 底座 |
| 单元 | 纯计算逻辑（无 UI、无 IO） | Vitest/Jest | pytest / JUnit 5 | ~10% |
| **集成** | **用户操作完整链路** | **RTL + MSW + Vitest** | **TestClient / MockMvc** | **~60%** |
| E2E | 关键端到端路径 | Playwright | Playwright / httpx | ~20% |

#### 什么是集成测试（vs 单元测试）

```typescript
// ❌ 单元测试：只测 hook，不知道页面是否正常工作
test('usePagination 返回正确页码', () => {
  const { result } = renderHook(() => usePagination())
  act(() => result.current.setPage(2))
  expect(result.current.page).toBe(2)        // 通过 ✅ 但用户点了没反应
})

// ✅ 集成测试：测用户操作完整链路
test('点击第 2 页显示第 2 页数据', async () => {
  render(<ListPage />)                        // 渲染完整页面
  await screen.findByText('第 1 条数据')       // 等数据加载
  await userEvent.click(screen.getByText('2')) // 模拟用户点击
  await screen.findByText('第 11 条数据')      // 验证数据切换了
})
```

区别在于：集成测试渲染**完整页面**，通过**用户视角操作**（点击、输入、选择），验证**用户能看到的结果**。中间的 state 变化、API 调用、数据处理都是自动串起来的——任何一环断了测试就失败。

#### 哪些功能 MUST 有集成测试

判断规则：**用户操作了但没反应，会被投诉的功能。**

| MUST 有集成测试 | 测试要点 |
|----------------|---------|
| 列表分页 | 点击页码 → 数据切换 |
| 搜索/筛选/排序 | 操作后 → 列表数据变化 |
| 表单提交 | 填写 → 提交 → 成功反馈（或错误提示） |
| CRUD 操作 | 新建→出现 / 编辑→保存→更新 / 删除→消失 |
| 状态切换 | Tab 切换、弹窗开关、展开收起 |
| 错误场景 | API 失败 → 显示错误提示（不是白屏） |
| 空状态 | 无数据 → 显示空状态提示 |

只对**纯计算函数**写单元测试：日期格式化、金额计算、数据转换、自定义校验规则。

#### 后端集成测试

后端同样的问题——单独测 service 函数通过，但走完整请求链路有 Bug：

```python
# ❌ 单元测试：只测 service 函数
def test_get_users():
    result = user_service.get_users(page=2)
    assert len(result) == 10                 # 通过 ✅ 但 API 实际返回空

# ✅ 集成测试：测完整请求链路
def test_get_users_api(client: TestClient):
    response = client.get("/api/users?page=2")  # 走路由→中间件→service→DB
    assert response.status_code == 200
    assert len(response.json()["data"]) == 10
```

| 后端框架 | 集成测试工具 | 测什么 |
|---------|------------|--------|
| FastAPI | `TestClient`（httpx） | 完整请求链路：路由→依赖注入→service→DB→响应 |
| Spring Boot | `MockMvc` / `WebTestClient` | 完整请求链路：Controller→Service→Repository→响应 |

#### API Mock 策略

前端集成测试需要 mock API（不依赖真实后端），推荐 **MSW（Mock Service Worker）**——在网络层拦截，组件代码完全不用改：

```typescript
// 测试中 MSW 拦截 API 请求，返回固定数据
http.get('/api/users', ({ request }) => {
  const page = new URL(request.url).searchParams.get('page')
  return HttpResponse.json({ data: mockData[page], total: 50 })
})
```

MUST NOT 直接 mock fetch/axios（绕过了组件内部的请求逻辑，测不到真实行为）。

---

### Step 3: Code（编码）

分步实施，**每步独立验证**：

```
任务规模：控制在 30 分钟人工等效工作量以内

顺序建议（TDD）：
1. 先写集成测试（定义用户视角的"完成"标准）
   - 涉及 UI 交互的功能 → 集成测试（渲染页面，模拟操作，验证结果）
   - 纯计算逻辑 → 单元测试
2. 实现代码，让测试通过
3. 修复边界情况（空值、异常输入、权限不足）
4. 重构（消除重复，保持高内聚低耦合）
```

**关键原则**：
- 如果 Claude 说"显著进展"但功能未完成，任务可能过大，需要拆分
- Claude 天然倾向"先写实现再写测试"——在 CLAUDE.md 或 prompt 中明确要求 TDD 顺序
- Claude 默认写单元测试——在 CLAUDE.md 完成标准中明确要求"关键用户交互 MUST 有集成测试"

### Step 4: Verify（验证）

Code 完成后，Claude 应**自动连续执行**以下验证（不需要你手动提醒）：

```
验证清单：
1. 运行集成测试和单元测试，修复所有失败
2. 确认关键用户操作链路有集成测试覆盖（分页、表单、CRUD 等）
3. 检查边界条件（空值、异常输入、权限不足、并发场景）
4. 运行 lint 和类型检查，修复所有警告
5. 运行全量测试，确认不影响现有功能（回归验证）
6. 前端：响应式和可访问性检查（如适用）
7. 后端：API 契约一致性和错误码规范（如适用）
```

**如何让 Claude 自动执行 Verify**：在项目 CLAUDE.md 中定义"完成标准"（参考文档 01 模板）。Claude 读到这些规范后，会在 Code 之后自动连续执行验证，形成 **Code→Verify 一条龙**。

**三层自动化闭环**：
- CLAUDE.md 完成标准 → Claude 主动验证
- PostToolUse Hook → 写文件时自动格式化/lint
- PreToolUse Hook → commit 前自动跑测试

### Step 5: Simplify（简化审查）

验证通过后，**在提交前跑 `/simplify`**：

```bash
/simplify
```

三个并行 agent 同时审查：代码复用、代码质量、执行效率。平均发现 3-5 个问题并自动修复，是提 PR 前的最后一道关卡。

### Step 6: Commit（提交）

Claude 自动执行 git add + commit（PreToolUse Hook 自动验证测试通过后提交），你只需确认 push：

```bash
git add <specific-files>   # 明确 add，不用 git add .
git commit -m "$(cat <<'EOF'
feat(auth): 添加 JWT 刷新 Token 机制

- 实现 refreshToken API 端点
- 添加 Token 过期自动刷新逻辑
- 前端 axios 拦截器集成

🤖 Generated with Claude Code
EOF
)"
```

### 进度跟踪：ROADMAP

整个开发循环之外，项目级进度通过 `docs/roadmap/` 目录持久化跟踪：

```
docs/roadmap/
├── README.md              ← 总览：Phase 列表 + 当前进度
├── phase-1-基础功能.md     ← 已完成（不加载，节省 Token）
├── phase-2-核心业务.md     ← 当前 Phase（CLAUDE.md @引用）
└── phase-3-高级功能.md     ← 未开始
```

- **初始化时**：Claude 用 subagent 探索项目全貌，自动生成路线图，你确认内容
- **日常开发**：Claude 通过 `@` 引用自动看到当前 Phase 进度，知道做到哪了
- **功能完成时**：完成标准自动更新 checkbox 状态 + Spec 状态（或手动 `/done`）
- **`/handoff` 时**：会话交接，确认 checkpoint 状态
- **`/catchup` 时**：自动读取路线图，恢复整体进度感知

粒度建议：**功能模块级**，每个 Phase 3-8 个条目，不拆到代码级。

### 六层收尾模型（v3.14）

开发中有六个粒度的"完成"：

| 层级 | 触发方式 | 动作 |
|------|---------|------|
| **Commit 级** | 自动（每次 `git commit`） | PreToolUse Hook 运行测试 + lint 门禁 |
| **实施级** | 手动（`/implement 描述`） | 硬阈值评估 → 模式扫描 → 执行 → Commit 前自检（Kent Beck 红灯 + Tidy First） → commit，不需要 /done（v3.19） |
| **功能/Spec Phase 级** | 手动（`/done 描述`） | 9 步 checklist：工作区/测试覆盖/Roadmap/Spec/文档/simplify + AskUserQuestion 决策点，**不重跑**测试（v3.20） |
| **Spec 完成级** | `/done` Gate 验证（所有 Phase 通过） | Spec `status→implemented` + 弹窗询问是否刷新 /docs |
| **文档同步级** | `/done` 弹窗触发 或 手动 `/docs` | 深度探索代码 → 梳理刷新开发文档（架构/上手/部署） |
| **Roadmap Phase 级** | `/done` 弹窗询问 → `/release` 执行 | 系统性文档刷新（`/docs` 全量 + Changelog + ADR）+ Phase 状态更新 |

**日常记住四个命令**：`/implement` 单改动实施、`/done 描述` 交付检查清单、`/docs` 文档更新、`/release` Phase 里程碑。

- `/implement 描述` — 有纪律的单改动实施（不需要 Spec 的小需求、Bug、微调，强制模式扫描 + 架构自检）
- `/done 描述` — 功能 commit 后的 9 步交付检查清单（测试覆盖 / Roadmap / Spec / 文档 / simplify），用弹窗处理决策点
- `/docs` — 随时可用，深度探索代码并刷新开发文档
- `/release` — Phase 全部完成时，系统性文档刷新 + Changelog + ADR

### Spec 进度自检协议（v3.9）

当基于 Spec 实施功能时，Claude 应在每次完成一个 task 后主动检查进度。在项目 CLAUDE.md 的"完成标准"部分加入以下规则：

```markdown
## Spec 实施自检

基于 docs/specs/ 中的 spec 实施功能时，MUST 遵循：
1. 每完成一个 task → 在 spec 文件中勾选 [x]
2. 当前 Phase 所有 Tasks 勾完 → 逐条检查 Gate 条件
3. Gate 全通过 → 更新 spec frontmatter 的 active_phase，提醒执行 /done
4. /done 检测完成粒度：Spec Phase 完成 / Spec 全部完成 / Roadmap Phase 完成
5. 上下文超过 50% 且当前 Phase 未完成 → 提醒用户考虑 /handoff → /clear → /catchup
```

**自动 vs 询问**：Claude 自动完成 `[x]` 勾选和 Gate 检查。`/done` 自动推进状态（`active_phase`、`status`），用 AskUserQuestion 询问决策点（补测试 / 跑 /docs / 跑 /simplify / 跑 /release），不自动执行重操作。

**与 PreCompact Hook 配合**：即使 auto-compact 意外触发，spec 文件中的 `[x]` 状态和 `active_phase` 已经持久化，compact 后 Claude 读取 spec 即可恢复进度感知（详见文档 02 PreCompact Hook）。

---

## 2. Plan Mode 使用规范

Plan Mode 是最重要的生产力工具之一，**复杂任务必用**。

### 进入 Plan Mode

```bash
Shift+Tab         # 三模式循环：Normal → Plan → Auto
/plan             # 直接进入 Plan Mode
```

### Plan Mode 的价值

| 维度 | 数据 |
|------|------|
| Token 节省 | ~40%（分析阶段不产生工具调用） |
| 错误率降低 | 明显减少（先规划再实施） |
| 并发安全 | Plan Mode 下可以并发多个会话，无修改代码风险 |

### 何时使用 Plan Mode

✅ **必须使用**：
- 修改多个文件的功能
- 不熟悉的代码区域
- 有架构影响的变更
- 调试复杂 Bug 前的分析

✅ **推荐使用**：
- 任何"改这个会影响什么"的场景
- 引入新依赖前

❌ **不需要**：
- 单文件简单修改
- 明确知道改哪里、怎么改

### Plan Mode 实践技巧

```bash
# 1. 在 Plan Mode 中让 Claude 充分探索
"列出所有会受影响的文件和模块"

# 2. 要求 Claude 说明假设
"你假设 X 是这样工作的，这个假设正确吗？"

# 3. 确认后再切换到 Normal 模式执行
Shift+Tab  # 切换回 Normal Mode
"好，按方案 2 实施"

# 4. 在编辑器中编辑 plan
Ctrl+G             # 在编辑器中打开 plan，可删除不需要的步骤、添加调整注释
```

### Auto Mode（Research Preview）

Shift+Tab 现在可以在三个模式间循环：**Normal → Plan → Auto**。Auto mode 由分类器自动判断权限——安全操作自动放行，危险操作拦截。适合对项目熟悉后想减少确认弹窗的场景。

### Ultraplan（Research Preview）

云端规划系统——本地发起规划请求，交给 claude.ai 上的 Claude Code 在 Plan Mode 中执行。终端保持空闲可做其他工作，规划完成后在浏览器中审阅（支持 inline comments），然后选择在云端实现或发回终端本地实现。

```bash
/ultraplan 将认证系统从 sessions 迁移到 JWTs
# 或在 Plan 审批对话框中选择 "No, refine with Ultraplan on Claude Code on the web"
```

### 混合模型策略

`opusplan` 模型别名：Plan 阶段自动使用 Opus（深度推理），切换到执行时自动换 Sonnet（速度快）。

---

## 3. 并行开发：Git Worktrees

Git Worktrees 允许同时运行多个 Claude Code 实例，每个实例在独立分支上工作，互不干扰。

### 基本用法

```bash
# 方法 1：Claude Code 内置 worktree 支持
claude --worktree feature-auth       # 在新分支 feature-auth 上启动隔离会话
claude --worktree bugfix-login       # 同时另开一个会话处理 Bug

# 方法 2：手动 git worktree
git worktree add ../project-auth feature-auth
cd ../project-auth
claude
```

### 并行开发场景

**前端+后端并行**：

```bash
# 终端 1：处理前端
claude --worktree feat/login-ui

# 终端 2：同时处理后端 API
claude --worktree feat/login-api
```

**功能开发+Bug 修复并行**：

```bash
# 终端 1：新功能开发（可以占用较多时间）
claude --worktree feat/dashboard

# 终端 2：紧急 Bug 修复（不影响功能开发）
claude --worktree fix/critical-bug
```

### Worktrees 最佳实践

- 每个 Worktree 有独立的上下文窗口（200k each）
- Auto Memory 在所有 Worktrees 间共享（同一个 git repo）
- project `.claude/settings.json` 被所有 Worktrees 继承
- 用 `/tasks` 追踪跨 Worktree 的整体进度
- 自定义 Agent 可通过 `isolation: worktree` 声明式创建隔离 Worktree
- Hook 状态中可获取 Worktree 信息（name、path、branch、原始仓库路径）

### 多 Claude 验证模式

```bash
# Claude A：实现功能
claude --worktree feat/new-feature

# Claude B：独立审查（新鲜视角，没有实现偏见）
claude --worktree review/new-feature
"请审查 feat/new-feature 分支的变更，找出潜在问题"
```

---

## 4. Agent Teams：多 Claude 协作

> **状态**：Experimental（默认禁用），可在生产中使用，API 仍在演进。

Agent Teams 允许一个 Claude 作为 Lead，协调多个 Teammate Claude 并行工作，每个 Teammate 有独立的上下文窗口。

### 启用方式

```json
// .claude/settings.json
{
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  }
}
```

### 工作机制

```
Lead Claude
  ├── 分解任务、分配给 Teammates
  ├── Teammate A（独立上下文）→ 实现模块 X
  ├── Teammate B（独立上下文）→ 实现模块 Y
  └── Teammate C（独立上下文）→ 写测试
       ↓
  汇总结果，解决冲突，最终 commit
```

**关键特性**：
- Teammate 自动加载同一个 CLAUDE.md、MCP servers、Skills
- Teammate **不继承** Lead 的对话历史，从空白开始（仅有 spawn prompt）
- 支持 Teammate 之间直接通信
- **Display modes**：`teammateMode` 支持 `in-process`（默认，全在一个终端）和 `tmux`/iTerm2 split panes
- **Plan approval**：可要求 teammate 先规划再实施，Lead 审批后才能改代码
- **Shift+Down** 可在 teammates 间循环切换、直接发消息

### 适合场景

| 场景 | 效果 |
|------|------|
| 大型功能拆分为独立模块并行实现 | 显著加速 |
| 前端+后端+测试同步开发 | 有效 |
| 代码库范围的统一重构 | 优先用 `/batch` |
| 简单单文件修改 | 杀鸡用牛刀，不推荐 |

### ⚠️ 成本警告

每个 Teammate = 独立的 Token 消耗。5 人团队至少 5x Token。**只在任务确实可以并行时才用**。

### TaskCompleted Hook：强制完成标准

配合 Agent Teams 使用，确保每个 Teammate 完成的任务必须通过质量门禁：

```json
"TaskCompleted": [
  {
    "hooks": [
      {
        "type": "command",
        "command": "bash .claude/hooks/task-complete-check.sh"
      }
    ]
  }
]
```

```bash
#!/bin/bash
# .claude/hooks/task-complete-check.sh
# 任务标记完成前，强制验证测试通过

if ! pnpm test --passWithNoTests 2>&1; then
  echo "❌ 任务不能标记完成：测试未通过" >&2
  exit 2
fi
exit 0
```

---

## 5. MCP 服务器选型

MCP（Model Context Protocol）让 Claude 连接外部工具和数据。选择合适的 MCP 服务器可以显著提升效率，但**过多 MCP 服务器会大量消耗上下文**。

### ⚠️ 重要提示：Token 成本

每个 MCP 服务器的工具定义默认会加载到上下文中。当工具描述总量超过上下文 10% 时，Claude Code 会自动启用 **MCP Tool Search**（按需懒加载），可减少高达 95% 的上下文占用。MCP 服务器支持 `list_changed` 通知，动态更新工具/资源时无需重连。可通过 `ENABLE_TOOL_SEARCH=false` 强制全量加载。MCP server 可对单个工具设置 `anthropic/maxResultSizeChars`（上限 500K 字符）保留大 payload。

**原则**：
- 只启用当前项目真正需要的 MCP 服务器
- 使用 `/context` 命令定期审查上下文占用
- 使用 `/mcp` 对话框式管理（启用/禁用/重连/OAuth 授权）

### 推荐的 MCP 服务器

**Context7（强烈推荐）**

将实时、版本精确的库文档注入上下文，防止 Claude 使用过时 API：

```bash
# 方式一：npx（推荐）
claude mcp add context7 -- npx -y @upstash/context7-mcp@latest

# 方式二：HTTP transport
claude mcp add --transport http context7 https://mcp.context7.com/mcp
```

适合：项目用到快速迭代的框架（React、Next.js、FastAPI）

---

**GitHub MCP**

直接在 Claude Code 中操作 GitHub：读取 Issue、管理 PR、触发 CI/CD：

```bash
claude mcp add --transport http github https://api.githubcopilot.com/mcp/ \
  --header "Authorization: Bearer $GITHUB_TOKEN"
```

**何时使用**：频繁需要读取 Issue 内容、创建/审查 PR 时。
**何时不用**：直接用 `gh` CLI 命令更高效时。

---

**Sequential Thinking**

提供结构化的逐步推理，适合架构决策和复杂问题分析：

```bash
claude mcp add sequential-thinking npx -y @modelcontextprotocol/server-sequential-thinking
```

适合：复杂架构决策、难以复现的 Bug 分析

---

### 不推荐用 MCP 替代的场景

| 需求 | 推荐方式（比 MCP 更高效） |
|------|------------------------|
| 文件操作 | Claude Code 内置文件工具 |
| Git 操作 | 直接 Bash（`git` 命令） |
| GitHub 简单查询 | `gh` CLI |
| 数据库查询（简单） | `psql`/`mysql` CLI |
| 部署操作 | `aws`/`gcloud` CLI |

### MCP 配置（项目级共享）

```json
// .mcp.json（版本控制，团队共享）
{
  "mcpServers": {
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp"
    }
  }
}
```

```bash
# 查看当前 MCP 状态
/mcp

# 检查上下文占用
/context
```

---

## 6. GitHub Actions 集成

Claude Code 提供官方 GitHub Action（`anthropics/claude-code-action`），实现 PR 自动代码审查和 Issue 自动实现。

### 快速安装

```bash
# 在 Claude Code 中一键安装
/install-github-app
```

### PR 自动代码审查

```yaml
# .github/workflows/claude-review.yml
name: Claude Code Review
on:
  pull_request:
    types: [opened, synchronize]
  issue_comment:
    types: [created]

jobs:
  review:
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude'))
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
```

### 使用方式

| 触发 | 效果 |
|------|------|
| 提交 PR | Claude 自动审查代码，在 PR 中留评论 |
| PR 评论 `@claude` | Claude 响应具体问题 |
| Issue 评论 `@claude implement this` | Claude 自动实现功能并创建 PR |

### Remote Control

从手机/平板/浏览器继续本地 Claude Code 会话：

```bash
claude remote-control              # 终端显示 QR 码
claude remote-control --name "功能开发"  # 自定义会话名称（便于区分多个实例）
# 扫码后在 claude.ai/code 或 Claude App 中继续同一会话
```

适合场景：离开电脑后继续监控或轻量交互。限制：终端需保持开启，每实例一个连接。

### Chrome 浏览器集成（v3.12）

Claude Code 可连接 Chrome 浏览器扩展，实现前端实时调试和 UI 验证：

```bash
claude --chrome                # 启动时连接 Chrome
/chrome                        # 会话中连接/管理 Chrome
```

**适合场景**：

| 场景 | 做法 |
|------|------|
| 前端 Bug 调试 | 读取 Console 错误和 DOM 状态，直接定位问题 |
| UI 设计验证 | 对比设计稿与实际渲染效果 |
| 表单测试 | 自动填写表单、触发验证逻辑 |
| 认证测试 | 通过已登录的 Chrome 会话访问 Google Docs、Notion 等 |

**工作流示例**：

```
你：登录页面的按钮颜色不对，帮我看看

Claude：（通过 Chrome 读取当前页面 DOM）
  检测到按钮使用了 .btn-secondary 而非 .btn-primary...
  已修改 LoginForm.tsx 第 42 行的 className
  请刷新浏览器查看效果。
```

> **注意**：需要安装"Claude in Chrome"浏览器扩展。Chrome 集成会增加上下文消耗，仅在需要浏览器交互时启用。

### Plugin 系统（v3.12）

Claude Code 支持插件生态，通过 `/plugin` 命令管理：

```bash
/plugin install <name>         # 安装官方市场插件
/plugin uninstall <name>       # 卸载插件
/reload-plugins                # 重新加载所有插件（无需重启）
```

**插件可以包含**：Skills、Agents、Hooks、MCP 服务器、LSP 服务器、配置。

**命名空间隔离**：插件中的 Skill 通过 `/<plugin-name>:<skill-name>` 调用，避免与项目自定义 Skill 冲突。

**何时使用**：
- 需要团队共享的审查规则或工作流 → 打包为插件分发
- 需要第三方集成（代码审查、安全扫描等）→ 从市场安装

> 插件系统是 Claude Code 的扩展生态，自定义 Skills（`.claude/skills/`）仍然是项目级工作流的首选方式。

### Computer Use（Research Preview）

Claude Code 可直接控制桌面——打开 native app、点击 UI、截图验证。Desktop app 和 CLI 都支持。

```bash
/mcp                          # 找到 computer-use → 开启
```

适合场景：iOS Simulator 测试、GUI-only 工具验证、无 API 的应用操作。

---

## 7. 开发文档体系

项目中有些信息无法从代码自动推断（怎么部署、怎么跑起来、为什么这样选型），需要手写文档。另一些信息（API 接口、数据模型）代码本身就是文档，不应该重复手写。

### 7.1 目录结构

```
docs/development/
├── getting-started.md     # 手写：新人上手指南（环境搭建、项目运行）
├── deployment.md          # 手写：部署流程、环境变量、回滚方案
└── changelog.md           # 自动生成：从 git commit 生成版本发布记录

docs/architecture/
├── README.md              # 架构总览（模块划分、整体数据流、非直觉设计，30-50 行）
├── frontend.md            # 前端架构（路由/组件分层/状态流转/交互模式，50-100 行）
├── backend.md             # 后端架构（请求链路/认证流程/状态机/异步任务，50-100 行）
└── adr/
    ├── README.md          # ADR 索引
    ├── 001-状态管理选型.md # 手写：为什么选 Zustand 而非 Redux
    └── 002-认证方案选型.md # 手写：为什么选 JWT 而非 Session
```

> **不单独写 API 文档和数据库文档**：FastAPI 自带 `/docs`（Swagger UI），Spring Boot 配合 `springdoc-openapi` 自动生成。数据库结构看 ORM 模型定义（SQLAlchemy models / JPA Entity）。手写会重复且容易过时。在 CLAUDE.md 中指明源码位置即可（如 "API 定义见 `apps/api/routers/`，数据模型见 `apps/api/models/`"）。

### 7.2 各文档内容规范

**`getting-started.md`** — 新成员能独立跑通项目：

```markdown
# 快速上手

## 环境要求
- Node.js >= 20, pnpm >= 10
- Python >= 3.12
- PostgreSQL >= 17
- [其他依赖]

## 首次运行
1. 克隆仓库 + 安装依赖
2. 配置环境变量（复制 .env.example → .env）
3. 初始化数据库
4. 启动开发服务器
5. 验证：访问 http://localhost:3000

## 项目结构概览
[各目录用途简要说明]

## 开发约定
[Git 分支策略、代码风格、PR 流程等]
```

**`deployment.md`** — 部署流程（代码里看不出来的信息）：

```markdown
# 部署文档

## 环境列表
| 环境 | URL | 分支 | 说明 |
|------|-----|------|------|
| 开发 | localhost | feature/* | 本地开发 |
| 测试 | staging.example.com | develop | 集成测试 |
| 生产 | example.com | main | 正式环境 |

## 部署流程
1. [CI/CD 流程说明]
2. [手动部署步骤（如有）]

## 环境变量
| 变量 | 必填 | 说明 | 示例 |
|------|------|------|------|
| DATABASE_URL | 是 | 数据库连接串 | postgresql://... |

## 回滚方案
[紧急回滚步骤]
```

**`changelog.md`** — 从 git commit 自动生成（[Keep a Changelog](https://keepachangelog.com/) 格式）：

```markdown
# Changelog

## [Unreleased]
### Added
- [新增功能]

## [1.0.0] - 2026-03-10
### Added
- 用户认证系统（登录/注册/Token 刷新）
- 仪表盘首页
### Fixed
- [修复内容]
```

> `/release` Skill 会自动从 `git log` 中提取 `feat:`、`fix:`、`refactor:` 等提交，按 Keep a Changelog 格式生成。

**ADR 模板**（`docs/architecture/adr/NNN-标题.md`）：

```markdown
# ADR NNN: [决策标题]

## 状态
[proposed | accepted | deprecated | superseded by ADR-XXX]

## 背景
[为什么需要做这个决策？]

## 决策
[选了什么方案？]

## 备选方案
[考虑过但没选的方案，附简要理由]

## 影响
[这个决策带来的影响：优势、劣势、技术债]
```

**架构认知地图**（`docs/architecture/README.md`）— 描述项目"怎么组织的"，而非"能做什么"：

```markdown
# 架构概览

## 模块划分
[顶层目录各自职责，划分原则（按业务域 / 按技术层）]
[模块间依赖关系]

## 前端架构（如有）

### 页面布局体系
- **模板页**：使用 `AppLayout` 组件（含导航栏、侧边栏）
  - dashboard、settings、profile 等页面
- **独立页**：不使用模板，自管布局
  - login、register、404 等页面
- 判断依据：看 app/ 路由下是否套了 layout.tsx

### 组件分层
- `components/ui/` — 纯 UI，无业务逻辑，可跨项目复用
- `components/business/` — 含业务逻辑，项目内复用

### 样式约定
- 全局通用 → `styles/globals.css`
- 组件级 → `xxx.module.css` 跟随组件
- 页面间共享业务样式 → `styles/pages/`

## 后端架构（如有）

### 模块组织
[按业务域 / 按技术层，每个模块的职责边界]

### 数据处理约定
[Converter/Transformer 在哪、Service 边界、事务管理方式]

### 关键非直觉设计
[例：库存扣减用数据库乐观锁不用 Redis — 原因：并发量级不到，减少依赖]

## 数据流
[请求从前端到数据库的完整路径，关键中间层]
```

**编写原则**：

| 写 | 不写 |
|---|---|
| 模块划分原则和理由 | 具体函数实现 |
| 页面用不用模板的判断依据 | 某个组件的 props 列表 |
| 数据流向和关键中间层 | 具体 SQL 语句 |
| 跨多文件才能串起来的逻辑链路 | 标准 CRUD 逻辑 |
| 非直觉的约定（及原因） | API 端点列表（看自动生成的文档） |
| 技术选型理由 | 依赖版本列表（看 package.json） |

**目录结构**：`README.md` 是架构总览（30-50 行），`frontend.md`（50-100 行）和 `backend.md`（50-100 行）分别记录前后端的详细逻辑链路。项目简单时可只用 README.md。

**与 CLAUDE.md 的关系**：CLAUDE.md 控制在 150 行以内，放不下架构全貌。在 CLAUDE.md 中用 `@docs/architecture/README.md` 引用，Claude 每次会话自动加载。

**核心价值**：Claude Code 每次 explore 是在还"认知债"。这份文档读一遍，就不需要反复 explore 项目组织方式了。对人类开发者同理——隔一段时间回来也需要这份认知地图。

---

### 7.3 代码即文档：不需要手写的部分

以下信息**从代码中直接获取**，不需要单独建文档文件：

| 信息 | 代码来源 | Claude 怎么找 |
|------|---------|-------------|
| API 接口定义 | FastAPI 路由 / Spring Controller | CLAUDE.md 指明路径：`apps/api/routers/` |
| 数据库结构 | ORM 模型（SQLAlchemy / JPA Entity） | CLAUDE.md 指明路径：`apps/api/models/` |
| 数据库变更历史 | Alembic 迁移 / Flyway 脚本 | CLAUDE.md 指明路径：`apps/api/migrations/` |
| API 在线文档 | FastAPI `/docs`、springdoc `/swagger-ui` | getting-started.md 中注明访问地址 |

在 CLAUDE.md 的"项目结构"部分加一行即可：

```markdown
- `apps/api/routers/` — API 路由定义（FastAPI 自动生成文档：http://localhost:8000/docs）
- `apps/api/models/` — 数据模型定义（SQLAlchemy ORM）
- `apps/api/migrations/` — 数据库迁移脚本（Alembic）
```

### 7.4 何时更新

| 时机 | 谁负责 | 更新什么 |
|------|--------|---------|
| **项目初始化** | Explore 后自动生成 | 架构认知地图（`docs/architecture/`） |
| **日常开发**（`/docs`） | 手动，随时可用 | 深度探索代码 → 增量刷新架构/上手/部署文档 |
| **功能完成**（`/done 描述`） | 手动 | 9 步交付检查清单：Roadmap/Spec 状态更新 + 测试覆盖扫描 + 文档影响弹窗（触碰架构敏感区时询问是否 /docs）|
| **Phase 完成**（`/release`） | 手动 | 系统性刷新（`/docs` 全量 + Changelog + ADR） |
| **深度审计**（`/docs audit`） | 手动 | 文档生态守护：spec-code 一致性、ADR 有效性、Gate 可执行性 |

> **原则**：只维护代码中无法体现的信息。能从代码自动推断的（API、数据模型），不重复写文档。

### 7.5 与现有文档系统的关系

```
docs/
├── roadmap/          # 进度跟踪（/handoff、/done 自动更新）
├── specs/            # 功能设计（/spec 生成，/done 更新状态）
├── development/      # 开发文档（手写 + 自动生成，/docs 刷新）
└── architecture/
    ├── README.md     # 架构总览（/docs 刷新+审计）
    ├── frontend.md   # 前端架构详细（/docs frontend 刷新）
    ├── backend.md    # 后端架构详细（/docs backend 刷新）
    └── adr/          # 架构决策记录（手写，/release 检查是否需要新增）
```

---

## 8. 上下文管理

### 8.1 监控原则：70% 规则

```
上下文使用率 < 70%：正常工作
上下文使用率 70-85%：计划会话边界
上下文使用率 > 85%：质量会明显下降，应该主动处理
```

```bash
# 查看当前上下文使用情况
/cost        # Token 使用统计
/context     # 上下文可视化（彩色格子显示）
```

> **关于 1M 上下文窗口**：Opus 4.8 和 Sonnet 4.6 支持 1M token 上下文（`/model opus[1m]`），Max/Team/Enterprise 计划默认包含。以上百分比阈值基于 200k 窗口，**使用 1M 窗口时原则不变**——主动分段优于被动 compact。注意：1M 窗口下 auto-compact 存在已知 bug（可能在远低于预期阈值时触发），建议同样遵循 70% 规则。

### 8.2 Document-and-Clear 模式（推荐）

**比 `/compact` 更可靠**的上下文管理方式：

```bash
# 1. 在会话到 70% 时，让 Claude 保存进度
"请将当前工作状态写入 .claude/session-notes.md，包括：
 - 已完成的工作
 - 关键决策
 - 下一步计划"

# 2. 清空上下文
/clear

# 3. 恢复（两种方式）
/catchup                     # 如果安装了 catchup Skill
# 或手动恢复
"请读取 .claude/session-notes.md，继续之前的工作"
```

### 8.3 应对 Auto-Compact（v3.9）

Auto-compact 在上下文达到约 83%（~167k token）时自动触发，会有损压缩早期对话。以下策略降低 compact 带来的影响：

**策略一：Spec 文件即进度（最根本）**

基于 spec 实施时，Claude 边做边在 spec 文件中勾 `[x]`、更新 `active_phase`。这些状态写在磁盘上，compact 不影响。Compact 后 Claude 重新读 spec 就能恢复进度。

**策略二：PreCompact Hook 自动保存（兜底）**

配置 PreCompact Hook（详见文档 02），在 auto-compact 触发前自动提醒 Claude 保存关键信息到 `.claude/session-notes.md`。

**策略三：调整 compact 触发阈值（进阶）**

```bash
# 环境变量：控制 auto-compact 触发百分比（1-100）
export CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=90
# 默认 ~83%，设高可延迟触发但留给压缩的 buffer 更小
# 建议范围：80-90，不建议超过 95
```

**策略四：主动分段优于被动 compact**

不要等 auto-compact，在 70% 时主动 `/handoff` → `/clear` → `/catchup`，比被动 compact 的信息保留质量高得多。

### 8.4 为什么不推荐 /compact

`/compact` 是上下文的有损压缩，社区反馈常见问题：
- 压缩后 Claude 忘记之前纠正的错误
- 重复之前修复过的 Bug
- 关键的"为什么这样做"信息丢失

如果必须使用 `/compact`，指定保留重点：

```bash
/compact 请保留：当前正在实现的功能、最近做的架构决策、还未完成的任务列表
```

### 8.5 多会话并行策略

对于大型任务，用多个独立会话比单个大会话更高效：

```bash
# 前端会话（200k 上下文专注前端）
claude --worktree feat/dashboard-ui
# → 只加载 apps/web/ 相关文件

# 后端会话（200k 上下文专注后端）
claude --worktree feat/dashboard-api
# → 只加载 apps/api/ 相关文件

# 总效果：400k 有效上下文，各自专注
```

---

## 9. 子代理与并发任务

### 9.1 内置子代理

Claude Code 内置了几个专用子代理：

| 子代理 | 适用场景 |
|--------|---------|
| `Explore` | 快速代码库搜索（只读，速度快） |
| `Plan` | Plan Mode 下的信息收集 |
| `General-purpose` | 复杂多步骤任务 |

### 9.2 自定义子代理

适合将重复的审查任务封装为子代理：

```yaml
# .claude/agents/code-reviewer.md
---
name: code-reviewer
description: 审查代码变更，检查质量和潜在问题
tools: Read, Grep, Glob, Bash
model: sonnet
permissionMode: plan              # plan 模式：只分析不修改
maxTurns: 20                      # 限制最大轮次
memory: project                   # 跨会话记忆（记住审查偏好和项目约定）
isolation: worktree               # 在隔离 worktree 中运行
mcpServers:                       # 限定可用的 MCP 服务器
  - context7
---

你是一位资深代码审查员。审查提供的代码变更：
1. 检查逻辑错误
2. 检查安全漏洞
3. 检查性能问题
4. 给出具体改进建议
```

**v3.12 新增 Agent 字段**：

| 字段 | 用途 |
|------|------|
| `memory: user/project/local` | 子代理跨会话持久记忆 |
| `isolation: worktree` | 声明式 Git Worktree 隔离 |
| `mcpServers` | 限定可用的 MCP 服务器 |
| `skills` | 预加载 Skills 到子代理上下文 |
| `background: true` | 始终在后台运行 |
| `disallowedTools` | 工具黑名单 |

### 9.3 后台任务

对于耗时操作，使用后台任务：

```bash
Ctrl+B          # 将当前任务转为后台运行
Ctrl+T          # 查看任务列表
/tasks          # 任务管理
```

### 9.4 Dynamic Workflows（动态工作流，2026 年中随 Opus 4.8 推出）

9.1–9.3 是"手动并发"——你逐个派子代理。**Dynamic Workflows** 把它升级为"用一段 JavaScript 脚本**确定性地**编排几十到几百个子代理"：循环、条件、扇出（fan-out）全写在脚本里，由代码控制而非模型临场决定，因此结果可复现、规模可控。

**何时用**（区别于普通子代理）：

- **规模化覆盖**：迁移所有调用点、审计整个代码库、跨大量文件批量改
- **多视角交叉验证**：N 个独立 agent 各自审查，再对抗式核对后才采信结论
- 单个上下文装不下的大任务

**核心编排原语**（脚本里可用）：

| 原语                                   | 作用                                                          |
|----------------------------------------|---------------------------------------------------------------|
| `agent(prompt, opts)`                  | 派一个子代理，返回其结果（带 `schema` 时强制结构化输出）        |
| `pipeline(items, stage1, stage2, ...)` | 每个 item 独立流过多个阶段，阶段间无屏障（**默认首选**）        |
| `parallel(thunks)`                     | 并发跑一批任务，全部完成才返回（屏障）                          |
| `phase(title)` / `log(msg)`            | 进度分组 / 进度输出                                            |

**触发方式**：

- **`ultracode` 关键词**：prompt 里带上它，Claude 会默认用工作流编排做这次任务（追求最详尽、最正确的结果，不计 token 成本）
- **`/deep-research <问题>`**：内置工作流——多源搜索 + 抓取来源 + 对抗式核验 + 产出带引用的报告
- **`/workflows`**：查看运行中和已完成的工作流

> ⚠️ **成本警告**：一个工作流可能 fan-out 出几十个子代理，token 消耗极大。**MUST 由用户显式选择**才启用（带 `ultracode` 关键词 / 明确要求"用工作流" / 调内置工作流命令），不会自动触发。与 [§4 Agent Teams](#4-agent-teams多-claude-协作) 同理：适合"值得用规模换质量"的大任务，不适合日常小改。

---

## 10. 代码质量防御（v3.13）

> AI 生成的代码维护性问题比人工代码多 1.64 倍、逻辑错误多 1.75 倍（GitClear/Qodo 2025 数据）。初始速度收益在两个月后被代码复杂度完全抵消（CodeScene 研究）。**不做质量防御的 AI 编码 = 高速生产技术债**。

### 10.1 核心问题

Claude 在修 Bug 和迭代调整时，优先级是**功能正确 > 代码规范**。每一轮"修→验→再修"都可能引入妥协性代码：内联样式、复制粘贴、绕过封装、临时 hack。单次看没问题，累积起来就是屎山。

更关键的是：CLAUDE.md 中的规则会随上下文增长被"遗忘"（早期指令权重随对话变长而降低）。但 **Hook 是确定性的**——每次都触发，不受上下文劣化影响。

### 10.2 分层防御模型

| 优先级 | 层 | 机制 | 拦什么 | 特点 |
|--------|---|------|--------|------|
| 1 | **Hook 硬拦** | Stop Hook 质量门禁 | 内联样式、@ts-ignore、HACK 标记等致命问题 | 确定性，永不遗忘 |
| 2 | **上下文纪律** | 迭代 3-4 轮未解决 → 主动清理上下文 | 上下文劣化导致的代码质量下降 | 预防根因 |
| 3 | **CLAUDE.md 红线** | 编码红线规则（< 10 条） | 复制粘贴、绕过封装、临时方案 | 基线约束，可能被遗忘 |
| 4 | **结构审查** | `/simplify`（功能完成后） | 重复逻辑、命名、冗余代码 | 兜底清理 |

### 10.3 Hook 硬拦（最重要）

在 Stop Hook 中检查本轮修改的文件是否有致命质量问题（详见文档 02 Section 5.4）。

**关键原则**：
- 只检查**最致命的几项**（3-5 个检查），不要变成完整 linter
- 检查项根据技术栈定制（前端：inline style / any；后端：裸 SQL / Controller 业务逻辑）
- 用 stdout 反馈给 Claude（非阻断），Claude 看到后会立即修正

### 10.4 上下文纪律

研究表明上下文质量从 20-40% 就开始渐进下降。虽然 70% 仍然是合理的行动线（在此之前 Hook 兜底），但在**迭代修 Bug 场景**下要更积极：

```
迭代修 Bug 超过 3-4 轮还没解决？
  ↓
不要在劣化的上下文里硬磕
  ↓
/handoff → /clear → /catchup
  ↓
用干净的上下文重新定位问题
```

**一个任务一个上下文窗口**——这不只是上下文管理技巧，更是代码质量保障。

### 10.5 CLAUDE.md 红线规则

在 CLAUDE.md 中写**少而硬**的编码红线（详见文档 01 的红线规则模板）。

**编写原则**：
- **少**：5-8 条，不要写成规范手册（CLAUDE.md 总共才 150 行预算）
- **硬**：用 MUST NOT，不是 SHOULD NOT
- **具体**：`MUST NOT 使用 style={{}}`，不是"保持代码整洁"
- **包含修 Bug 场景**：明确写"修 Bug 时同样适用"
- **技术栈专属规则放 `.claude/rules/`**：按路径加载，不浪费 Token

### 10.6 /simplify 兜底

功能完成后执行 `/simplify`，做三维并行审查（复用性、质量、效率）。这是最后一道防线，处理"能工作但不够优雅"的代码——重复逻辑提取、命名优化、冗余判断简化。

### 10.7 为什么不用专门的"重构 Skill"

`/simplify` 已经覆盖了结构审查。与其事后重构，不如：
- 加强 Hook 硬拦（源头防止脏代码产生）
- 保持上下文干净（防止 Claude 因劣化写出脏代码）

事后清理的本质问题是"代码已经烂了再修，成本更高"，而且实际操作中"等做完再清理"往往就不清理了。

---

## 11. 代码健康诊断（v3.15）

> 不开发新功能，专门做系统性代码质量审查和重构规划——填补"功能开发后顺便检查"与"积累到难以维护才重构"之间的空白。

### 11.1 为什么需要独立的诊断工作流

现有的审查工具各有侧重，但没有覆盖"代码架构与长期可维护性"：

| 工具 | 时机 | 关注点 | 缺口 |
|------|------|--------|------|
| `/simplify` | 功能完成后 | 本次改动质量 | 不看全局 |
| `/audit` | 每周 | 项目卫生（lint/依赖） | 不看架构 |
| `/docs audit` | Phase 后、需要深度审计 | 文档生态守护（spec-code / ADR / Gate）| 不看代码结构 |
| Stop Hook | 每次响应 | 致命问题 | 只检查 3-5 项 |
| **`/diagnose`** | **独立会话** | **13 维度全覆盖** | — |

**核心理念**（Jeff Atwood）：*"Every new line of code you willingly bring into the world is code that has to be debugged, read, and supported."* 代码不是越多越好——**实现相同功能，复杂度越低越好**。`/diagnose` 的目标就是找到那些不必要的复杂度（Fred Brooks 所说的 accidental complexity），并规划消除它们。

### 11.2 两阶段工作流

```
阶段一：诊断（/diagnose）
  → 全维度扫描 → 量化评分 → 问题清单 → 分批重构计划
  → 输出 docs/reports/diagnose-YYYY-MM-DD.md
  → 不改代码

阶段二：执行（/implement 批量模式）
  → 读取诊断报告，按 Batch 顺序执行
  → 每个 Batch：补测试 → 重构 → 验证 → commit
  → 全部完成后再次 /diagnose 验证改善
```

**为什么分两阶段**：重构最怕"改了一堆但不知道改对了没"。先诊断出全貌、确认优先级，再分批执行，每批有明确验收标准。

### 11.3 13 维度概览

| 层级 | 维度 | 一句话 |
|------|------|--------|
| 结构 | D1 耦合度 | 改 A 会不会崩 B |
| 结构 | D2 职责划分 | 每个单元是否只做一件事 |
| 结构 | D3 模块边界 | 通过接口还是深入内部 |
| 结构 | D4 依赖方向 | 有没有循环依赖、反向依赖 |
| 实现 | D5 代码重复 | 近似逻辑是否散落多处 |
| 实现 | D6 错误处理 | 是否一致且完整 |
| 实现 | D7 类型安全 | 类型系统是否被正确使用 |
| 实现 | D8 性能隐患 | N+1、不必要 re-render、内存泄漏 |
| 实现 | D9 测试覆盖 | 关键操作链路是否有集成测试 |
| 卫生 | D10 死代码 | 未使用的函数/导入/变量 |
| 卫生 | D11 一致性 | 同一件事是否用同一种方式做 |
| 战略 | D12 代码热点 | 改动频繁 × 质量差的交叉区域 |
| 战略 | D13 知识孤岛 | bus factor = 1 的关键模块 |

完整的维度检查指南和 Skill 模板见文档 03 Section 5.9。

### 11.4 大项目的 SubAgent 策略

小项目（< 50 源文件）主 Agent 直接扫全维度。中大项目按模块拆分 SubAgent 并行：

```
SubAgent A → 前端/模块1 × 全部 13 维度
SubAgent B → 后端/模块2 × 全部 13 维度
SubAgent C → 跨模块问题（耦合、依赖方向、API 契约）
  ↓
主 Agent 汇总 → 去重 + 优先级排序 → 完整报告
```

每个 SubAgent 有独立 200k 上下文，维度不砍、范围不砍、上下文不爆。

### 11.5 推荐频率

| 项目阶段 | 频率 | 原因 |
|----------|------|------|
| 快速迭代期 | 每月 | 迭代快，技术债积累快 |
| 稳定维护期 | 每季度 | 变更少，风险低 |
| 大功能开发前 | 开发前 1 次 | 提前识别要碰区域的健康度 |
| 感觉"代码变难改了" | 随时 | 直觉往往是对的 |

### 11.6 与分层防御模型的关系

Section 10 的分层防御是**被动防御**——在开发过程中防止代码劣化。`/diagnose` 是**主动进攻**——定期检查并清除已积累的技术债。两者互补：

```
被动防御（Section 10）        主动诊断（Section 11）
Hook 硬拦 ─────────────────→ 防止新问题产生
上下文纪律 ─────────────────→ 防止劣化环境下写烂代码
CLAUDE.md 红线 ────────────→ 基线约束
/simplify 兜底 ────────────→ 功能级清理
                             /diagnose 定期全面体检
                             → 发现存量问题
                             → 规划系统性消除
```

防御再好也会有漏网之鱼。`/diagnose` 就是定期把漏网的鱼捞出来。

---

## 12. 反模式：避免这些坑

### 12.1 上下文相关

**❌ 让上下文超过 85% 才处理**

症状：Claude 开始重复已改的错误，代码质量下降
解决：在 70% 时主动执行 Document-and-Clear

**❌ 依赖 /compact 管理长会话**

症状：压缩后 Claude 忘记重要的约束和决策
解决：改用 Document-and-Clear 模式

**❌ CLAUDE.md 超过 200 行**

症状：Claude 遵从度下降，某些规则被忽略
解决：拆分到子目录 CLAUDE.md 或 .claude/rules/

---

### 12.2 执行相关

**❌ 不让 Claude 先探索就直接要求实现**

症状：Claude 实现的代码与现有代码冲突，需要大量返工
解决：始终先 Explore，再 Plan，最后 Code

**❌ 单次任务范围过大**

症状：Claude 声称"已完成"但实际有大量功能缺失
解决：将任务拆分到 30 分钟人工等效工作量以内

**❌ 让 Claude 不经门禁直接 git 操作**

症状：merge 混乱，误 commit 无关文件
解决：通过 PreToolUse Hook 自动验证（测试通过才允许 commit）；git push 需你确认；force push / reset --hard 直接拒绝

**❌ Claude 修改测试文件来让测试通过**

症状：测试通过但实现是错的
解决：在 CLAUDE.md 加入 `MUST NOT 修改测试文件以适配错误的实现`；用 PreToolUse Hook 监控

**❌ 代码写完就当"完成"，不做验证**

症状：Claude 报告"功能已实现"，但边界条件未处理、lint 有警告、回归测试未跑
解决：在 CLAUDE.md 定义"完成标准"（测试通过 + lint 通过 + 边界条件覆盖 + 回归验证），Claude 会自动在 Code 后连续执行 Verify

---

### 12.3 配置相关

**❌ 启用过多 MCP 服务器**

症状：每次请求消耗大量 Token，上下文快速耗尽
解决：只启用真正需要的 MCP；用 `/context` 监控

**❌ 过度 Hook（对每个操作都挂钩）**

症状：社区案例：3 轮操作消耗 160k Token
解决：Hook 的 matcher 精确定位；只挂关键节点

**❌ 自造已有成熟库的功能**

症状：Claude 为常见需求编写复杂的自定义实现
解决：在 CLAUDE.md 明确列出应使用的库名（如 `日期处理 MUST 使用 dayjs`）

---

### 12.4 期望相关

**❌ 期望 Claude 第一次就完全正确**

实际：有经验的开发者估计成功率约 50%，需要迭代
正确期望：像和初级开发者协作——给方向、验结果、给反馈

**❌ 代码不 Review 就合并**

实际："基本正确"的代码可能有微妙的 Bug（边界条件、并发场景）
解决：AI 写的代码必须经过人工 Review，不能跳过

---

## 13. 快速参考卡

### 每日工作流

```bash
# === 会话开始 ===
# SessionStart Hook 自动执行（显示 git 状态）

# === 开发功能 ===
Shift+Tab           # 进入 Plan Mode
"探索 [相关文件]，不要写代码"
"基于探索，提出 2-3 个实现方案"
# 复杂功能：此时上下文可能已 50%+
# "将方案写入 .claude/plan.md" → /clear → "读取 plan.md，按方案实施"
Shift+Tab           # 切换回 Normal Mode
"按方案 2 实施，先写测试"
# Claude 自动：Code → Verify（跑测试 + 边界检查 + lint）

# === 功能完成后 ===
/simplify           # 三维并行审查并自动修复（PR 前必跑）
/done 完成了用户登录  # 9 步交付检查清单 + 弹窗决策（可能自动带动 /docs、/release）
/docs               # 如 /done 里跳过了，手动刷新开发文档

# === 监控上下文 ===
/cost               # 查看 Token 使用
/context            # 上下文可视化

# === 上下文接近 70% ===
/handoff            # 提交当前变更 + 生成交接文档
/clear              # 清空
/catchup            # 恢复

# === 提交工作 ===
# Claude 自动 git add + commit（Hook 自动验证测试通过后提交）
# 你只需确认 push

# === 单改动实施 / Bug 修复 ===
/implement 列表加排序功能  # 单改动，直接做（强制模式扫描 + 架构自检）
/implement                 # 批量模式：列出多个改动逐个处理
"[描述 Bug 现象]"          # 也可直接描述，走常规 Bug 修复流程
```

### 批量变更（新）

```bash
/batch "将所有 moment.js 替换为 dayjs，更新调用语法"
# → 自动分解 → 并行 worktree agent → 各自 PR
```

### 每周维护

```bash
/audit              # 标准健康检查
# 检查 CLAUDE.md 是否过时（手动）
# 检查 Auto Memory 是否需要清理（/memory）
```

### Phase 完成后

```bash
/release            # 系统性文档刷新（/docs 全量 + Changelog + ADR）+ Phase 状态更新
/diagnose           # 代码架构量化（13 维度 + 重构计划，独立会话）
```

> 完整命令速查见文档 00 Section 7。

### 模型选择建议

| 场景 | 推荐模型 |
|------|---------|
| 日常编码 | Opus 4.8（当前默认旗舰）或 Sonnet 4.6（更省，简单改动） |
| 复杂架构设计 | Opus 4.8（最强推理之一，默认 high effort） |
| 长程自动化 / 大规模重构 | Fable 5（Mythos 级，最强） |
| 简单格式化/搜索 | Haiku 4.5（快速低成本） |
| 需要深度思考 | Opus 4.8 + `/effort xhigh`（或 `ultrathink` 关键词） |

```bash
/model              # 切换模型
/effort             # 设置思考深度（low/medium/high/xhigh/max/auto）
/fast               # 切换 Fast Mode（更快输出）
```

> **Effort 级别**：`low`（快速简答）、`medium`（默认，日常编码）、`high`（深度思考，复杂任务）、`max`（仅当前轮次最大思考）。也可在 prompt 中用 `ultrathink` 关键词临时提升。

---

**版本**: v3.41
**更新日期**: 2026-06（v3.41）
