# 日常使用说明

> 实际使用这套系统的完整指南 — 新手到熟练的全流程

**版本**: v3.14
**适用**: Claude Code 2.x（2026 年）

---

## 目录

1. [第一次：新项目初始化](#1-第一次新项目初始化)
2. [日常开发流程](#2-日常开发流程)
3. [项目路线图（ROADMAP）](#3-项目路线图roadmap)
4. [Git 操作说明](#4-git-操作说明)
5. [上下文管理：何时用 /handoff /catchup /clear](#5-上下文管理)
6. [新开会话 vs 继续任务](#6-新开会话-vs-继续任务)
7. [旧命令对照表](#7-旧命令对照表)
8. [所有命令速查](#8-所有命令速查)
9. [常见场景处理](#9-常见场景处理)

---

## 1. 第一次：新项目初始化

只需要做一次，之后就不用管了。

```bash
# 1. 进入项目根目录，启动 Claude Code
claude

# 2. 让 Claude 读指南并帮你配置
"请阅读 ~/Downloads/00_project/guides/ 下的所有文档，
 然后根据我的项目帮我完成初始化配置。
 我的项目技术栈是：[前端 React + 后端 FastAPI + PostgreSQL，pnpm 管理]"
```

Claude 会自动完成：
- 运行 `/init` 生成 `CLAUDE.md` 草稿
- 创建 `.claude/settings.json`（权限 + Hooks 配置）
- 创建 `.claude/hooks/` 脚本文件
- 创建 `.claude/skills/` 目录（audit、deep-audit、catchup、handoff、spec、task、done、docs、release、nbp2）
- 按需创建子目录 `CLAUDE.md`

你只需要检查 `CLAUDE.md` 内容是否准确，精简到 150 行以内。

---

## 2. 日常开发流程

### 正常的一天

```
打开终端
   ↓
claude                          ← 启动 Claude Code
   ↓
SessionStart Hook 自动运行      ← 自动显示 git 状态，无需手动做任何事
   ↓
直接说需求，开始开发             ← "帮我实现用户登录功能"
   ↓
Claude 自动：
  - 先探索相关代码（不直接写）
  - 提出方案，等你确认
  - 实现代码（TDD：先写测试 → 实现 → 验证）
  - 验证：跑测试 + 边界检查 + lint（自动连续完成）
  - 简化审查：代码复用性 + 质量 + 效率检查
  - git commit（Hook 自动验证后提交）
   ↓
继续下一个功能，或关闭
```

**你需要手动做的事：几乎没有。**

唯一需要注意的是 git push：Claude 会在 push 前弹出确认，你说"可以"就推。

---

### 开发一个功能的标准节奏

```
你：帮我实现 [功能描述]

Claude：（自动进入探索）
  读取相关文件...
  当前代码结构是这样的...
  我有 2 个实现方案：方案 A... 方案 B...
  你更倾向于哪个？

你：用方案 A

Claude：好，按方案 A 实施，先写测试...
  [TDD：写测试 → 实现代码 → 测试通过]
  [Verify：边界条件检查 → lint/类型检查 → 全量测试回归验证]
  验证全部通过，功能已实现。

你：/simplify

Claude：[启动三个并行审查 agent → 自动修复 3 个问题]
  审查完成，已修复：重复的工具函数提取、变量命名优化、冗余判断简化。

  [git commit → Hook 自动验证测试通过后提交]
  commit 已创建：feat(auth): 实现用户登录功能

你：push 一下

Claude：执行 git push... （弹出确认）

你：确认

Claude：推送完成。
```

---

## 3. 项目路线图（ROADMAP）

### 是什么

`docs/roadmap/` 目录是项目的持久化进度跟踪系统，解决"Claude Code 跨会话不知道项目做到哪了"的问题。

```
docs/roadmap/
├── README.md              ← 总览：Phase 列表 + 当前聚焦的 Phase
├── phase-1-基础功能.md     ← 已完成的 Phase
├── phase-2-核心业务.md     ← 当前进行中
└── phase-3-高级功能.md     ← 未开始
```

### 你需要做什么

**几乎不需要手动维护**：

| 场景 | 谁来做 |
|------|--------|
| 初始化路线图 | Claude 探索项目后自动生成，你确认内容 |
| 更新进度（checkbox） | `/handoff` 时 Claude 自动更新 |
| 添加新功能 | 你在对话中说"加到路线图里"，Claude 更新 |
| 切换 Phase | Claude 更新文件 + 你更新 CLAUDE.md 的 `@` 引用 |

### CLAUDE.md 中的引用

```markdown
@docs/architecture/README.md
@docs/roadmap/README.md
@docs/roadmap/phase-2-核心业务.md
```

架构文档始终引用；路线图只引用总览 + 当前 Phase，已完成的 Phase 不加载，节省 Token。

Phase 推进时更新引用即可。

---

## 4. Git 操作说明

### Claude 自动处理的操作

| 操作 | 触发时机 | 是否需要你确认 |
|------|---------|--------------|
| `git add <文件>` | 功能实现完成时 | 否 |
| `git commit` | add 之后，测试通过后 | 否（Hook 自动验证）|
| `git push` | 你说"push"时 | **是，弹出确认** |

### Claude 不会自动做的操作（需要你说）

| 操作 | 为什么 |
|------|--------|
| `git push --force` | 直接拒绝，必须你自己执行 |
| `git reset --hard` | 直接拒绝，必须你自己执行 |
| 切换/创建分支 | 不会主动做，但你说了它会执行 |

### 关于 git add

Claude 会用 `git add <具体文件>`，不会用 `git add .`，防止误提交：
- 临时调试文件
- `.env` 敏感文件
- 构建产物

---

## 5. 上下文管理

### 什么是上下文满了

Claude Code 每个会话有 200k Token 的上下文窗口。随着对话越来越长，窗口会被填满。满了之后 Claude 的表现会变差（忘记之前的约束、重复之前修复过的错误）。

### 如何查看上下文用量

```bash
/cost       # 查看 Token 使用统计
/context    # 可视化上下文占用（彩色格子）
```

### 三个阶段的处理方式

```
0% - 70%   正常工作，不用管
            ↓
70% - 83%  开始规划会话边界
           "我们当前做到哪了？还有什么没完成？"
            ↓
           /handoff          ← 让 Claude 把进度写入文件
           /clear            ← 清空上下文，重置为 0%
           /catchup          ← 读取进度文件，恢复状态
            ↓
~83%       auto-compact 自动触发（有损压缩，可能丢失细节）
           如果配置了 PreCompact Hook，会先保存进度到文件
            ↓
85%+       质量会明显下降，此时才处理已经晚了
           （所以要在 70% 时主动处理）
```

> **提示**：如果你基于 Spec 实施功能，Claude 会在 spec 文件中实时勾选 `[x]` 和更新 `active_phase`。即使 auto-compact 触发，这些状态都在磁盘上，不会丢失。

### /handoff → /clear → /catchup 的具体操作

```bash
# Step 1：保存进度
/handoff
# Claude 会把"做到哪里、决策了什么、还剩什么"写到 .claude/session-notes.md

# Step 2：清空上下文
/clear

# Step 3：恢复
/catchup
# Claude 读取 session-notes.md，快速重建状态，继续工作
```

整个过程大约 1-2 分钟，之后可以继续工作，上下文重置为 0%。

---

## 6. 新开会话 vs 继续任务

这是最容易混淆的地方，对照来看：

### 场景一：新开会话，开始新功能

```bash
# 昨天的功能已经 commit 了
# 今天开始新的功能

claude                # 启动
# Hook 自动运行，显示 git 状态
# Auto Memory 自动加载，Claude 记得你的偏好

"今天我们来做用户权限管理功能"    # 直接开始，不需要任何命令
```

**不需要 `/catchup`**，Auto Memory + CLAUDE.md 已经提供了足够的上下文。

---

### 场景二：新开会话，继续昨天未完成的任务

```bash
# 昨天任务做到一半，执行了 /handoff 后关闭
# 今天继续

claude                # 启动
/catchup              # 读取昨天的 session-notes.md，接上昨天的进度
```

**需要 `/catchup`**，因为有具体的"做到哪一步"的上下文需要恢复。

---

### 场景三：会话中上下文接近满了

```bash
# 在同一个会话中，/cost 显示接近 70%

/handoff              # 保存进度
/clear                # 清空
/catchup              # 恢复，继续工作
```

---

### 判断要不要 /catchup 的规则

```
上次关闭前有没有运行 /handoff？
  ├── 没有 → 不需要 /catchup，直接开始
  └── 有   → 要不要继续那个任务？
              ├── 不继续 → 不需要 /catchup
              └── 继续   → 需要 /catchup
```

---

## 7. 旧命令对照表

### 完整对照

| 旧命令 | 旧用途 | 新方式 | 是否需要手动触发 |
|--------|--------|--------|----------------|
| `/start` | 每次会话开始恢复上下文 | SessionStart Hook + Auto Memory | ❌ 自动 |
| `/checkpoint` | 阶段性完成 → 更新文档 + commit | Claude 直接 commit | ❌ 自动 |
| `/end` | 每天结束 → 更新文档 + commit + push | Claude 直接 commit/push + 可选 `/handoff` | ✅ push 需确认 |
| `/weekly` | 每周文档优化 | 不再需要（CLAUDE.md 不会膨胀） | ❌ 废弃 |
| `/monthly` | 每月归档 | 不再需要（Auto Memory 自动管理） | ❌ 废弃 |
| `/fix` | ESLint 自动修复 | PostToolUse Hook 写文件时自动格式化 | ❌ 自动 |
| `/audit` | 健康检查 | `/audit`（保留，升级为 Skill） | ✅ 手动触发 |
| `/deep-audit` | 深度审计 | `/deep-audit`（保留，升级为 Skill） | ✅ 手动触发 |
| — | — | `/catchup`（新增） | ✅ 按需 |
| — | — | `/handoff`（新增） | ✅ 按需 |

### 总结

```
废弃（不再需要）：/start  /checkpoint  /end  /weekly  /monthly  /fix
保留（手动触发）：/audit  /deep-audit
新增（按需使用）：/catchup  /handoff
```

---

## 8. 所有命令速查

### 高频命令

| 命令 | 何时用 |
|------|--------|
| `/simplify` | **每次功能完成后、PR 前**（自动代码审查并修复） |
| `/task` | 日常小任务执行（`/task 加排序` 单任务 / `/task` 批量模式） |
| `/done` | 智能收尾检查（附描述：`/done 完成了用户登录`） |
| `/docs` | 深度探索代码，梳理更新开发文档（`/docs` 全量 / `/docs backend` 按范围） |
| `/release` | **Phase 完成后**，系统性文档刷新 + 生成 Changelog |
| `/handoff` | 任务做一半要中断；或上下文接近 70% |
| `/catchup` | 继续昨天未完成的任务；或 /clear 后恢复 |
| `/spec` | 需求讨论到一定程度后，整理为设计文档 |
| `/clear` | 配合 /handoff 使用，重置上下文（别名 /reset、/new） |
| `/insights` | 会话分析报告（交互模式、项目区域、摩擦点） |
| `/audit` | 每周健康检查 |
| `/audit --quick` | 快速检查，提交前 |
| `/audit --security` | 上线前安全扫描 |
| `/deep-audit` | Phase 完成后全面代码审计 |
| `/plan` | 进入 Plan Mode，复杂任务前先规划 |
| `/cost` | 查看上下文使用量 |
| `/context` | 可视化上下文占用 |

### Bundled 命令（内置，共 5 个，无需配置）

| 命令 | 何时用 |
|------|--------|
| `/simplify` | 三维并行代码审查并自动修复（每次 PR 前必跑，也列于高频命令） |
| `/batch <描述>` | 跨多文件的统一变更（替换库、批量重命名、统一格式） |
| `/debug` | 交互式调试助手（设置断点、分析堆栈、定位根因） |
| `/loop <间隔> <prompt>` | 定时重复执行（如 `/loop 5m /audit --quick`，默认 10 分钟间隔，最长 3 天，上限 50 个任务） |
| `/claude-api` | Claude API / Anthropic SDK 集成指导 |

示例：
```bash
/batch "将所有 axios 替换为 fetch API，更新请求/响应处理语法"
/loop 5m "检查 CI 状态，如果失败就通知我"
```

### 文档类 Skills

| 命令 | 何时用 |
|------|--------|
| `/docs` | 深度探索代码，梳理更新开发文档（日常高频，保持文档与代码同步） |
| `/docs architecture` | 只刷新架构文档（README + frontend + backend） |
| `/docs backend` | 只刷新后端架构文档 |
| `/docs getting-started` | 只刷新上手指南 |

### 工具类 Skills

| 命令 | 何时用 |
|------|--------|
| `/nbp2` | 编写 Nano Banana Pro 2 AI 生图 Prompt（六要素公式 + 进阶技巧） |

### 偶尔会用到的命令

| 命令 | 用途 |
|------|------|
| `/memory` | 查看/管理 Auto Memory |
| `/hooks` | 查看 Hook 配置 |
| `/mcp` | 管理 MCP 服务器（启用/禁用/重连/OAuth，对话框式操作） |
| `/skills` | 列出所有可用 Skills |
| `/copy` | 复制代码块（可交互选择具体代码块） |
| `/diff` | 查看当前未提交的变更 |
| `/rewind` | 回滚到某个时间点（可选仅回滚对话 / 仅回滚代码 / 两者同时） |
| `/fork` | 会话分支，探索不同方案 |
| `/voice` | 语音模式（Push-to-talk，空格键说话，支持 20 种语言） |
| `/model` | 切换模型（sonnet/opus/haiku） |
| `/effort` | 设置模型思考深度（low/medium/high/max/auto），影响输出质量和速度 |
| `/fast` | 切换 Fast Mode（Opus 4.6 快速输出，约 2.5x 速度） |

### 内置快捷键

| 快捷键 | 用途 |
|--------|------|
| `Shift+Tab` | 切换 Plan Mode / Normal Mode |
| `Ctrl+T` | 显示/隐藏任务列表 |
| `Ctrl+B` | 将当前任务转为后台运行 |
| `Space`（语音模式） | Push-to-talk（需先 `/voice` 启用） |
| `Esc` `Esc` | 快速回滚（Rewind） |

---

## 9. 常见场景处理

### 场景：任务做到一半，临时有事要离开

```bash
/handoff              # 保存进度
# 直接关闭终端

# 回来后
/catchup              # 接着做
```

---

### 场景：上下文满了，任务还没做完

```bash
/handoff              # 保存进度到 session-notes.md
/clear                # 清空上下文
/catchup              # 恢复，继续做
```

---

### 场景：今天的功能全部做完了，要收工

```bash
# 完成标准已自动更新 Roadmap + Spec 状态
# 如果想确认没有遗漏：
/done 完成了XX功能      # 智能收尾：代码验证 + Roadmap/Spec 状态更新
                        # Spec 全部完成 → status→implemented
                        # Roadmap Phase 也完成 → 建议执行 /release
"push 一下"            # Claude 执行 git push，弹出确认
# 确认后，直接关闭
# 不需要 /handoff（功能已完整 commit，明天从新功能开始）
```

---

### 场景：今天做到一半，要收工，明天继续

```bash
"先把当前改动 commit 一下，用 WIP 前缀"
# Claude commit：wip: 登录功能开发中

/handoff              # 记录"做到哪一步，明天继续什么"
# 关闭

# 明天
/catchup              # 接着昨天的状态继续
```

---

### 场景：处理业务方的小需求 / 小 Bug

```bash
# 方向明确、不需要讨论的小任务，用 /task 直接做

/task 列表页加个按名称排序的功能

# Claude 自动：评估复杂度 → Explore → Code → Verify → Commit
# ✓ [a1b2c3d] feat(list): 添加按名称排序功能

# 积攒了多个小任务？用批量模式
/task
# Claude: 请列出要处理的任务
# 你：
# 1. 日期格式从 MM/DD 改成 YYYY-MM-DD
# 2. 默认分页从 10 改成 20
# 3. 商品列表查询加索引
# Claude 逐个处理，每个独立 commit
```

什么时候用 /task 而不是直接说需求？当你有**明确的小改动**，想要结构化的评估→执行→验证→提交流程时。如果只是改个 typo，直接说就行。如果需要讨论设计，用 /spec。

---

### 场景：修复一个 Bug

```bash
# Bug 修复复用同一套开发循环，但侧重点不同

你："登录页面在手机端点击提交按钮没有反应"

# Claude 自动进入 Explore（复现+定位）：
#   读取登录组件、事件处理逻辑...
#   定位根因：onClick 使用了 onMouseDown，移动端不触发

你：确认，修一下

# Claude 进入 Code（回归测试+修复）：
#   1. 写复现测试（模拟触摸事件，断言提交被触发）
#   2. 修复：onMouseDown → onPointerDown
#   3. 复现测试变绿
#   4. Verify 自动执行（回归验证）

# git commit -m "fix(auth): 修复移动端登录按钮无响应"
```

Bug 修复和新功能的区别：Explore 是"复现+定位"而非"了解架构"，Code 先写**复现测试**而非功能测试。简单 Bug 可以跳过 Plan，复杂 Bug 建议用 Plan Mode 分析。

---

### 场景：发现 Claude 在重复犯同一个错误

```bash
# 例如：Claude 总是用 Redux，但你的项目用 Zustand

# 方法 1：更新 CLAUDE.md
# 在 CLAUDE.md 的约束里加一条：
# "MUST NOT 使用 Redux，状态管理 MUST 使用 Zustand"

# 方法 2：让 Auto Memory 记住
"记住：这个项目用 Zustand 管理状态，不用 Redux，以后不要再建议 Redux"
# Auto Memory 会自动记录这个纠正
```

---

### 场景：需求讨论了很多轮，上下文快满了

```bash
# 讨论了好几轮需求、搜了技术方案、敲定了 UI 设计...
# 上下文已经用了 50-60%，还没开始写代码

/spec user-auth       # 将讨论成果整理为 docs/specs/user-auth.md（含实施阶段）
/clear                # 清空上下文

# 如果还要继续讨论
"读取 docs/specs/user-auth.md，继续讨论权限模型部分"
# 讨论后再次 /spec 更新

# 如果讨论完了，开始分阶段实施
"读取 docs/specs/user-auth.md，开始实施 Phase 1"
# 每次只实施一个 Phase，完成 Gate 后再进入下一个
```

> `/spec` 支持增量更新——讨论可以跨多个上下文窗口，每次 `/spec` 会合并新内容到已有文件。

---

### 场景：大 Spec 实施中途 auto-compact 了

```bash
# 正在实施一个大功能的 Phase 2，突然触发了 auto-compact
# 不用慌——spec 文件中的 [x] 状态和 active_phase 都在磁盘上

# Compact 后 Claude 会自动读取 spec 恢复进度
# 如果 Claude 看起来迷失了方向，手动提示：
"读取 docs/specs/user-auth.md，查看 active_phase，继续未完成的 Tasks"

# 预防措施（推荐配置）：
# 1. 配置 PreCompact Hook（见文档 02），compact 前自动保存进度
# 2. 在 70% 时主动 /handoff → /clear → /catchup，比被动 compact 好得多
```

---

### 场景：复杂功能，不知道从哪下手

```bash
Shift+Tab             # 进入 Plan Mode

"我需要实现一个完整的权限管理系统，
 包括角色、权限、用户绑定，支持动态菜单。
 请先探索现有代码，然后给我一个实现方案，不要写代码。"

# Claude 探索代码，给出方案
# 你确认方案后

# 此时上下文可能已经 50-70%（探索+规划消耗大量 Token）
# 推荐：主动清理上下文，带 plan 开新会话
"将实施方案写入 .claude/plan.md"
/clear
"读取 .claude/plan.md，按方案实施"

# 如果上下文还充裕（< 40%），可以直接继续：
Shift+Tab             # 切回 Normal Mode
"按方案实施"
```

---

### 场景：每周健康检查

```bash
/audit                # 检查代码质量、依赖、文档同步
# 查看报告，按优先级处理问题
```

---

### 场景：某个 Phase 功能全部完成

```bash
/done 完成了最后一个功能    # 收尾检查，会提示 Roadmap Phase 全部完成
/release                   # 系统性文档刷新 + 生成 Changelog
/deep-audit                # 全面审计：代码、文档、依赖全部对一遍
```

---

### 场景：开发了一段时间，文档需要更新

```bash
/docs                      # 全量：探索代码 → 刷新所有开发文档
/docs backend              # 只刷新后端架构文档（刚改了很多后端逻辑）
/docs getting-started      # 加了新依赖，更新上手指南
```

---

## 附：与旧习惯的完整对比

### 你以前的一天

```
开始：/start                          ← 手动恢复上下文
开发：...
完成一个功能：/checkpoint --commit     ← 手动触发 commit
继续开发：...
结束：/end --push                     ← 手动触发 commit + push
周日：/weekly --push                  ← 手动优化文档
月初：/monthly --push                 ← 手动归档
```

### 你现在的一天

```
开始：claude                          ← Hook 自动运行，无需任何命令
开发：...
完成一个功能：Claude 自动 commit      ← 无需手动
继续开发：...
结束：确认 push                       ← 弹出一次确认即可
周日：/audit（可选）                  ← 如果想检查的话
月初：什么都不用做                    ← Auto Memory 自动管理
```

**每天节省的操作**：`/start`、`/checkpoint`、`/end` 共 3 次手动命令 → 全部自动化。

---

**版本**: v3.14
**更新日期**: 2026-03
